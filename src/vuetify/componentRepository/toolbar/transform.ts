import {widgetTransformUtil} from 'mttk-lowcode'

export function toolbarTransform(config: any) {
  //
 const result= widgetTransformUtil.buildWidgetWithSlotChildren("v-toolbar",config)

 return result
 
}
