import {widgetTransformUtil} from 'mttk-lowcode'

export function cardTransform(config: any) {
  //
 const result= widgetTransformUtil.buildWidgetWithSlotChildren("v-card",config)

 return result
 
}
