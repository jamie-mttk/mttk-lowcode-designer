import {widgetTransformUtil} from 'mttk-lowcode'

export function sheetTransform(config: any) {
  //
 const result= widgetTransformUtil.buildWidgetWithSlotChildren("v-sheet",config)

 return result
 
}
